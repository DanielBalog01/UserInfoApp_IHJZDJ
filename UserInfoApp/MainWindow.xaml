<Window x:Class="UserInfoApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UserInfoApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <StackPanel Margin="20">
            <TextBox Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>
            <TextBox Text="{Binding UserAge, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>
            <TextBox Text="{Binding UserEmail, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>
            <Button Content="Mentés" Click="SaveButton_Click" Margin="0,0,0,10"/>
            <Button Content="Megjelenít" Click="ShowButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
